package aula_33.excecoes.excecoesverificadas;

public class ExemploScannerProblema {

	public static void main(String[] args) {
		/*
		 * // Cria um objeto representando o arquivo em disco File arquivo = new
		 * File("c:\\temp\\arquivo.txt");
		 * 
		 * // Abre o arquivo usando o objeto arquivo Scanner sc = new Scanner(arquivo);
		 * 
		 * // Enquanto houver linhas a serem lidas while (sc.hasNextLine()) { // Lê a
		 * próxima linha e a mostra na tela System.out.println(sc.nextLine()); }
		 */
	}

}
